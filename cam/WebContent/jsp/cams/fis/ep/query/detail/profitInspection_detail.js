function init() {
	/*
	 * var ss = L5.getCmp("noProfit"); alert(ss); if(prop=='1'){
	 * L5.getCmp("profit").hidden = true; L5.getCmp("profit").hideMode="none"
	 * var ss = L5.getCmp("profit"); }else{ L5.getCmp("noProfit").hidden = true;
	 * L5.getCmp("noProfit").hideMode="none" }
	 * 
	 */
	L5.QuickTips.init();
}

function back() {
	history.go(-1);
}