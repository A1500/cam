function init() {	
	//ds.baseParams["PYBZY_ID@="]=idField;
	ds.load();
	L5.QuickTips.init(); 
}

 function back()
 {	/* 	var url='jsp/cams/marry/mrmpybzy_list.jsp';
		var text = '聘用颁证员';
		L5.forward(url,text);	 */
		window.close();
 }